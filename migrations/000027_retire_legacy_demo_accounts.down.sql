-- Intentionally irreversible: restoring predictable fixture credentials or
-- roles would reintroduce a production security risk. Restore from a backup
-- only when rolling back the entire release under an incident runbook.
SELECT 1;
